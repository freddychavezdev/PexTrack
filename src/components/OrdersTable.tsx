import type { WorkOrder } from '../types'
import { statusMeta } from '../lib/domain'
export function OrdersTable({ orders, selectedId, onSelect }: { orders: WorkOrder[]; selectedId?: string; onSelect: (order: WorkOrder) => void }) { return <div className="h-full overflow-auto"><div className="sticky top-0 z-10 flex items-center justify-between border-b border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-slate-900"><h2 className="font-semibold">Órdenes de trabajo</h2><span className="text-sm text-slate-500">{orders.length} activas</span></div><table className="w-full text-left text-sm"><thead className="sticky top-[53px] bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-800"><tr><th className="p-3">OT / Cliente</th><th className="p-3">Estado</th><th className="p-3">Cuadrilla</th></tr></thead><tbody>{orders.map(o => <tr key={o.id} onClick={() => onSelect(o)} className={`cursor-pointer border-t border-slate-100 hover:bg-blue-50 dark:border-slate-800 dark:hover:bg-slate-800 ${selectedId === o.id ? 'bg-blue-50 dark:bg-slate-800' : ''}`}><td className="p-3"><p className="font-medium">{o.codigo_ot}</p><p className="text-slate-500">{o.cliente_nombre}</p></td><td className="p-3"><span className={`rounded-full px-2 py-1 text-xs font-medium ${statusMeta[o.estado].className}`}>{statusMeta[o.estado].label}</span></td><td className="p-3 text-slate-500">{o.cuadrilla_nombre ?? 'Sin asignar'}</td></tr>)}</tbody></table></div> }
