import type { WorkOrder } from '../types'
import { statusMeta } from '../lib/domain'
export function OrdersTable({ orders, selectedId, onSelect }: { orders: WorkOrder[]; selectedId?: string; onSelect: (order: WorkOrder) => void }) { return <div className="orders-table h-full overflow-auto"><div className="orders-table__heading sticky top-0 z-10"><div><p className="text-xs font-semibold text-slate-500">Vista de asignaciones</p><h2>Órdenes de trabajo</h2></div><span className="orders-count">{orders.length} activas</span></div>{orders.length === 0 ? <div className="grid min-h-48 place-items-center px-6 text-center text-sm leading-6 text-slate-500">No hay órdenes activas para esta cuadrilla.</div> : <table className="w-full text-left text-sm"><thead className="sticky top-[76px] z-[1]"><tr><th>OT / Cliente</th><th>Estado</th><th>Cuadrilla</th></tr></thead><tbody>{orders.map(o => <tr key={o.id} onClick={() => onSelect(o)} className={`order-row ${selectedId === o.id ? 'order-row--selected' : ''}`}><td><p className="font-semibold text-slate-800 dark:text-slate-100">{o.codigo_ot}</p><p className="mt-1 line-clamp-1 text-slate-500">{o.cliente_nombre}</p></td><td><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${statusMeta[o.estado].className}`}>{statusMeta[o.estado].label}</span></td><td className="text-slate-500">{o.cuadrilla_nombre ?? 'Sin asignar'}</td></tr>)}</tbody></table>}</div> }
